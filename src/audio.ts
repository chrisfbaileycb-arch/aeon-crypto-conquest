class Audio{ctx?:AudioContext;muted=false;private init(){this.ctx??=new AudioContext();this.ctx.resume()}tone(freq:number,d=.08,type:OscillatorType='sine',vol=.12){if(this.muted)return;this.init();const o=this.ctx!.createOscillator(),g=this.ctx!.createGain(),t=this.ctx!.currentTime;o.type=type;o.frequency.setValueAtTime(freq,t);g.gain.setValueAtTime(vol,t);g.gain.exponentialRampToValueAtTime(.001,t+d);o.connect(g).connect(this.ctx!.destination);o.start();o.stop(t+d)}stop(i:number){this.tone(150+i*35,.1,'triangle',.18)}win(){[523,659,784,1047].forEach((n,i)=>setTimeout(()=>this.tone(n,.35,'square',.08),i*100))}toggle(){this.muted=!this.muted;return this.muted}}
export const audio=new Audio();
